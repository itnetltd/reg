{% if items %}
  <ul{{ attributes.addClass('utility-menu') }}>
    {% for item in items %}
      <li{{ item.attributes.addClass(item.in_active_trail ? 'menu-item--active-trail') }}>
        {{ link(item.title, item.url) }}
      </li>
    {% endfor %}
  </ul>
{% endif %}
