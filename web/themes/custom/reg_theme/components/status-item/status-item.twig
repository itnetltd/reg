<a class="status-item" href="{{ item.url|default('#') }}">
  <span><strong>{{ item.title }}</strong><small>{{ item.description }}</small></span>
  <time>{{ item.date }}</time>
  <span class="status-item__action">{{ 'Read details'|t }} →</span>
</a>
