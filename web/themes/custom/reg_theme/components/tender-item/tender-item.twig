<a class="tender-item" href="{{ item.url|default('#') }}">
  <span><strong>{{ item.title }}</strong><small>{{ item.description }}</small></span>
  <span class="tender-item__status">{{ item.status }}</span>
  <span class="tender-item__action">{{ 'View tender'|t }} →</span>
</a>
