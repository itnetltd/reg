<a class="service-card" href="{{ item.url }}">
  <span class="service-card__eyebrow">{{ item.eyebrow }}</span>
  <span class="service-card__symbol" aria-hidden="true">↗</span>
  <strong>{{ item.title }}</strong>
  <span>{{ item.description }}</span>
</a>
